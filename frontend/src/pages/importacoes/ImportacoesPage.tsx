import { PagePlaceholder } from "../../components/ui/PagePlaceholder";

// Conteúdo real implementado na Sprint 11 (IMPORTADOR.md, TELAS.md).
export function ImportacoesPage() {
  return (
    <PagePlaceholder
      titulo="Importações"
      descricao="O fluxo de upload, histórico e rollback de importações será implementado na Sprint 11."
    />
  );
}
