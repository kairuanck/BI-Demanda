import { PagePlaceholder } from "../../components/ui/PagePlaceholder";

// Conteúdo real (gestão de usuários) implementado na Sprint 11 (TELAS.md, seção 8).
export function ConfiguracoesPage() {
  return (
    <PagePlaceholder
      titulo="Configurações"
      descricao="A gestão de usuários e demais configurações será implementada na Sprint 11."
    />
  );
}
