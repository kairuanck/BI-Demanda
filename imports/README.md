# imports/

Diretório de armazenamento dos arquivos binários originais enviados nas importações (`importacao_arquivos.caminho_armazenamento`), correspondente a `STORAGE_DIR` (ver `DEPLOY.md`, seção 3, e `HASH.md`, seção 8). Não é versionado — ver `.gitignore` e `docs/DECISIONS.md`.
