# database/

Diretório onde o arquivo SQLite da aplicação (`app.db`) é criado em tempo de execução (local ou via volume Docker). Não é versionado — ver `.gitignore` e `docs/DECISIONS.md` (item sobre localização do banco).

Para detalhes de conexão e migração para PostgreSQL, ver `DATABASE.md` e `DEPLOY.md`.
